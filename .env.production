# API URL - use same-origin path in production to avoid CORS/domain drift
VITE_API_URL=/api/generate

# Frontend mock mode flag (keep false in production)
VITE_USE_MOCK_AUDIO=false

# ElevenLabs API key must be configured as server env var:
# ELEVENLABS_API_KEY=your_elevenlabs_api_key
